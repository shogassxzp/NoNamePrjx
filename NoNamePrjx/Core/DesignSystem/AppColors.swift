import SwiftUI

enum AppColors {
    static let background = Color(red: 0.04, green: 0.06, blue: 0.06)
    static let surface = Color(red: 0.10, green: 0.13, blue: 0.12)
    static let accent = Color(red: 0.18, green: 0.72, blue: 0.42)
    static let secondaryText = Color.white.opacity(0.68)
}
